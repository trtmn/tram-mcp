# macOS
.DS_Store

# Secrets / env
.env
.env.*

# Local MCP client config (coworker install is documented in the README, not committed)
.mcp.json
.claude/settings.local.json

# Built artifacts (.mcpb bundles are built by CI on release; do not commit)
*.mcpb

# Node / TypeScript
node_modules/
dist/
